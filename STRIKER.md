# Striker — 仿脑记忆与思考系统 (v1)

> **Striker**：意为"冲击时代进步的人"。这是独立于 RTorch 的记忆/思考系统。
> 命名分工：
> - **RTorch** = 通用计算框架（算）：Rust 前端 + C++ Vulkan 引擎 + `.rtw` 容器，负责
>   编译、调度、计算、计时。做"计算"。
> - **Striker** = 仿脑记忆系统（记/思）：负责**记忆、取舍、遗忘、状态与权重双轨**。
>   做"该记的记、该忘的忘、把自己塑造成前进的人"。
>
> 两者衔接：Striker 用 RTorch 做底层计算（状态演化、摘要编码、权重蒸馏的数值部分），
> 但记忆的**语义/管理逻辑**归 Striker。

## 0. 设计原则（用户确立）

1. **模型学的是状态，不是文本。** 文本/对话只是填充位置的观测，驱动状态，不被当作本体记住。
2. **该记的记、该忘的忘**——记忆是**主动取舍**，不是全量堆叠。
3. **模型自己知道该记该忘**——靠**可计算信号**（频率/新颖性/利用度）动态判定，不靠人贴标签。
4. **对话可忘可不忘、可再记**——记忆强度连续可升降，重现就升格，非二元"记/忘"。
5. **避开 KV cache**——不存原始 token 流，存压缩语义；不无限积累，不断消化。

## 1. 记忆架构：状态 + 权重 双轨

```
输入(用户话/对话)
   → ① 摘要器: 提炼语义要点(不存原始 token 流)      ← 拒 KV cache 第一步
        ↓  索引/压缩后的语义碎片
   ┌─── ② 状态轨(短期·活) ─────────────────────────┐
   │  摘要 → 更新记忆状态 s_m (仿脑状态演化/记忆池)    │
   │  ■ 可演化/可衰减/可升格:  "对话可忘可不忘, 可再记" │
   │  ■ 强:重复/相关→状态强度↑   弱:一次性→衰减→淡忘  │
   │  ■ 低但非零 → 重现升格(再记一次)                 │
   └────────────────────────────────────────────┘
        ↓ 权重计算(判定永久价值)
   ┌─── ③ 权重轨(长期·固化) ───────────────────────┐
   │  高价值状态摘 → 蒸馏 → 写入模型固定权重           │
   │  (= 训练根基, 受训练目标保护, 几乎不衰减)         │
   └────────────────────────────────────────────┘
```

**分工**：
- **状态轨**：处理活对话——临时用、用后可忘；有价值的保持强；重复的升格。= "现在知道什么"。
- **权重轨**：处理该永久沉淀——高价值状态蒸馏成权重进根基，下次直接用。= "固化了什么"。

**为什么双轨**：单状态轨无法把"临时活记忆"变成永久能力；单权重轨无法区分临时 vs 永久，
且对话全塞权重又成另一种 KVCache。双轨 = 活的状态记忆 + 固化的权重记忆，覆盖临时/永久两级。

### 1.1 记忆碎片：活对象，而非 embedding

**关键**：一个记忆碎片 memN **不只是向量/embedding**——它必须携带足够元数据，
才是一个"活东西"，能被 Striker 做取舍/升格/防遗忘/失效判断。至少包含：

```
memN = {
  semantic_state : 语义状态(内核, 可向量/结构)   // 记忆的内容表示
  strength       : 强度(牢固度)                 // 该记多牢, 驱动升格/淡忘
  creation_time  : 创建时间                     // 生命周期/时效判断
  last_recall    : 最近被召回时间               // 时效/遗忘曲线
  recall_count   : 被召回次数                   // 利用度信号
  novelty        : 新颖度                       // 是否新信息/覆盖度
  reliability    : 可靠性(多源/交叉印证一致度)   // 重复≠正确 的关键字段
  conflict       : 冲突度(与已有可靠记忆矛盾度)  // 防错误固化
  source_links[] : 来源链接(证据/出处)          // 可追溯, 有据可查
}
```

- `reliability` / `conflict`给 MemoryScore 打分用，**阻止"重复的错误被永久化"**。
- `source_links[]`让记忆**可追溯**（不是凭空向量），符合严谨/可验证。
- `strength`/`last_recall`/`recall_count` 支持遗忘曲线与升格。
- 这样的碎片是"**活记忆**"，能自我管理取舍，而不是一个死的 embedding 向量。

## 2. Striker"怎么知道"该记该忘：可算信号（数学定义）

不靠人规定，靠一组**可计算**信号。**关键：重复 ≠ 正确**，所以可靠性/一致性、冲突度
必须是**硬闸门**（前置否决），而不仅是 MemoryScore 里的加分项。

### 2.1 信号定义与归一化（各信号归一到 [0,1]）

| 信号 | 标记 | 归一化定义 | 说明 |
|---|---|---|---|
| 重复性 | `rep` | `rep = 1 - exp(-λ·count)` | count 为出现频次；λ>0 是饱和率，次数越多越接近 1 |
| 新颖性 | `nov` | `nov = 1 - sim_max` | `sim_max`=与该记忆最相似的已存碎片的相似度∈[0,1]；越新越接近 1 |
| 利用度 | `use` | `use = 1 - exp(-μ·recall_count)` | 被召回次数；μ>0 饱和率 |
| 目标相关性 | `rel` | `rel = (cos(s, t)+1)/2` | s 为状态/碎片向量，t 为目标表示，余弦相似度归一到 [0,1] |
| 可靠性 | `relb` | `relb = N_agree / N_src` | 交叉印证一致度：多源占比（N_src 个独立来源中 N_agree 个一致）∈[0,1] |
| 冲突度 | `cfl` | `cfl = max_j sim_contrast(s, m_j)` | 与所有已存可靠碎片的**对立度**最大值∈[0,1]；`sim_contrast`∈[0,1] 度量矛盾程度 |

### 2.2 硬闸门（前置否决，先于打分）

**可靠性/冲突度不是普通打分项，是独立闸门**——不通过直接拦截，与 MemoryScore 无关：

```
闸门条件（AND）:
  ①  relb ≥ R_min        （可靠性下限, 默认 R_min = 0.6）
  ②  cfl  ≤ C_max        （冲突度上限, 默认 C_max = 0.4）
  ③  存在 ≥ N_min 条独立来源佐证（默认 N_min = 2, 防单源）

若任一条不满足:
  → 该记忆**不进候选集**（即使 MemoryScore 再高）; 补足证据或解决冲突后才可重审。
```

### 2.3 MemoryScore（打分，仅在通过闸门后）

```
MemoryScore = w_rep·rep + w_nov·nov + w_use·use + w_rel·rel
            + w_relb·relb - w_cfl·cfl                      ← 可靠减冲突
Σ w = 1
默认权重: w_rep=.25  w_nov=.15  w_use=.15  w_rel=.15  w_relb=.20  w_cfl=.10
```

### 2.4 时间衰减（遗忘曲线）

```
strength(t) = strength₀ · exp(-γ·(t_now - t_last_recall))     γ>0 遗忘率
若 strength < θ_forget  → 该记忆淡忘(降权/可合并)            θ_forget 遗忘阈值
每次被召回: strength 以 +Δ 提升, 并刷新 t_last_recall (回放/强化)
```

### 2.5 处置（由 MemoryScore 推导，Striker 自判）

```
MemoryScore ≥ S_cand 且通过闸门    → 进【候选集】, 走 §4 加固流程
MemoryScore < S_cand 但复用/可靠    → 状态轨强保留 + 弱标记(可再记, 重现升格)
一次性/过时/冗余                   → 状态轨衰减 → 主动淡忘(降权/合并)
低但非零 + 重现 + 可靠性↑          → 强度回升(再记一次), 可进入候选
```

**关键**：类别（该记/可忘/可再记）由信号**动态算出**，非人贴标签。且**可靠性/冲突度以
闸门形式卡在打分与升格前**——**防止"重复的错误信息被当成高价值永久固化"**。
这是 Striker 与"真正会成长的系统"之间一道关键的门槛：**重复 ≠ 正确**。

## 3. 摘要→状态 （记忆核心形态）

对话 → 摘要器 → **语义要点**（取舍、不存原文）→ 状态编码器 → **记忆状态 s_m**。

- 状态 s_m 是记忆的**活表达式**，进入仿脑状态演化/记忆池。
- **天然压缩**：多个对话融合成一个状态，不留原文。
- **可演化/可衰减**：过时对话，状态相应弱化（该忘）。
- **可升格**：高价值状态成分强度高，再次被调用=强化（再记），升格固化（进权重）。
- **拒 KV cache**：任何对话先摘要（不存原始 token 流），进的是压缩状态/权重，不是原文堆。

## 4. 权重蒸馏：安全加固流程（不再"直接写权重"）

**重要修正**：高价值 State **不能直接写入固定权重**——否则若用户连续重复一个错误信息
（重复性↑+利用度↑ → 被判高价值 → 蒸馏进权重），**错误会被永久化**。
实际流程必须经**候选集 + 回放 + 增量训练 + A/B 回归测试 + 批准**才固化：

```
高价值 State
   → 【候选学习集】(未固化, 先留着)
   → 旧知识回放(防灾忘, 保旧知识)
   → 增量训练(把候选融入权重做一次小步更新)
   → A/B 回归测试 (旧知识性能 vs 固化前)
   → 旧知识性能 ≥ 阈值 → 批准固化
   → 否则: 拒该候选(或降级回状态轨), 权重不动
```

- **安全阀**：把"直接写权重"替换成"候选 + 验证 + 批准"。错误信息即使进入候选，
  也过不了 A/B 回归测试（它若与旧可靠知识冲突，性能不达标 → 不固化）。
- 可经**增量学习/蒸馏**路径（候选摘要作为小步更新/额外训练样本），可计算可训练。
- 与"训练不该忘"统一：固化前必须**回放旧知识**（防固化新知识时破坏旧根基），
  并用 A/B 回归测试确认旧知识性能不降。

### 4.1 候选→固化 的安全闸门

| 阶段 | 动作 | 目的 |
|---|---|---|
| 候选集 | MemoryScore 达候选阈值的 State 进入，**未固化** | 暂存，不立即改权重 |
| 旧知识回放 | 固化前回放旧碎片/旧任务样本 | 防"固化新知识破坏旧知识" |
| 增量训练 | 候选 + 回放一并做小步更新 | 可计算、可训练地提炼 |
| A/B 回归测试 | 固化后 vs 固化前，旧知识性能对比 | 验证旧知识没崩 |
| 批准/拒绝 | 旧性能 ≥ 阈值 → 批准固化；否则拒绝/降级 | 拦下错误的/冲突的记忆固化 |

## 5. 与 KV cache 之别（对外口径）

| | KV cache | Striker 仿脑记忆 |
|---|---|---|
| 存什么 | 原始 token 的 key-value 状态，逐字全量 | 压缩后的**语义碎片**（摘要/状态），不存原文 |
| 取舍 | 无——全部堆 | 主动取舍：高价值强化/升格，低价值衰减/淡忘 |
| 进化 | 只增不减，越多越慢 | 不断消化：该永久的→权重，该忘的→去掉，不无限涨 |
| 记忆 | 无分层，原地堆 | 分层：状态轨(活/短期) + 权重轨(固化/长期) |

**一句话**：KV cache 存"说过的话"，Striker 存"理解了的东西"，且分两层、会取舍、能消化。

## 5.1 防灾难性遗忘（关键机制）

**灾难性遗忘** = 学新东西时把旧知识彻底破坏（权重被新任务大幅改写，旧任务性能崩塌）。
这是所有序列/增量学习记忆的死穴——**Striker 作为会"记新忘旧"的系统，必须防住它**。

### 为什么 Striker 天然易患灾忘
- 记忆是**动态写入**的（对话不断更新状态/权重）。
- 若新写入**全量覆盖/大幅改写**旧记忆 → 旧知识被破坏。

### 三道防线（碎片化 + 回放 + 加固，仿人脑）

**① 碎片分区**（结构性防线，最根本）
- 记忆存为**多个独立碎片** memN（不是单一状态向量覆盖一切）。
- 新信息写入**新/独立碎片**或**低相关碎片**，旧碎片**基本不动** → 旧记忆不丢。
- 仿脑依据：人的长时记忆是**片段/联想索引**，不是单一覆盖状态。

**② 选择性回放/巩固**（学新时强化旧）
- 学新输入时，同时**回放一小撮旧碎片**（用旧样本/旧记忆重算一遍，强化其权重/强度）。
- 仿脑依据：人睡眠时海马体**重放当天记忆**，旧知识被反复强化、不易覆盖。
- 效果：旧记忆在学新的时被"复习"，既保住又更牢固。

**③ 弹性权重加固（EWC 类）**（保护重要参数）
- 给各参数标记"重要度"（旧任务/旧记忆越依赖 → 越重要）。
- 更新时对**高重要度参数约束**（别乱改），新记忆捡**低重要度参数**改。
- 仿脑依据：习得牢固的"突触"不易被改写。
- 效果：即使要改参数，也优先动"不重要的"，保护"根基认知"。

### 三道防线如何对应 Striker 的机制

| 防线 | 在 Striker 里的落点 |
|---|---|
| 碎片分区 | 状态轨的记忆池 = 多碎片；新摘要写新/低相关碎片，旧碎片不动 |
| 回放巩固 | 高价值状态被反复召回/强化时=回放；学习新对话时回放旧摘要 |
| 弹性加固 | 权重轨：训练根基/高重要度参数受保护，更新只动低重要度部分 |

### 灾难性遗忘的可测检验

学"任务A"后再连续学"任务B"、任务C……回到 A 测：**若 A 性能不掉 = 防住了**。
这是可测、可验证的判据（不是空谈），实现后用持续学习基准任务回归验证。

**总结**：Striker 三层防线——**碎片化(结构) + 回放(巩固) + 弹性加固(保护参数)**，
既像人的记忆取舍，又能防"学了新的忘旧的"。碎片化本身已是第一道墙，但**光碎片化不够**
（碎片间若仍共享基础参数，灾忘照样穿透），必须配**回放 + 加固**一起。

## 6. 与其他系统关系

| 系统 | 职责 | 存储/产物 |
|---|---|---|
| **RTorch** | 通用计算框架（编译/调度/计算/计时） | `.rtw` 容器、Vulkan/CPU 引擎 |
| **Striker** | 仿脑记忆与思考系统（该记该忘/状态权重双轨） | 记忆状态 s_m、蒸馏权重、记忆池 |

Striker 是"冲击时代进步的人"——它的任务不是算，是**积累、取舍、塑造成长的记忆**，
经 RTorch 提供的算力把"值得记住的"沉淀、"该忘的"淡忘，让自己越来越像一个不断进步的人。

## 7. 待实现清单（下一步）

1. **摘要器**：对话→语义要点（取舍，不存全文）。
2. **状态轨**：摘要→记忆状态 s_m（仿脑），可演化/衰减/升格。
3. **活碎片**：memN 按 §1.1 字段（semantic_state/strength/creation/last_recall/recall_count/novelty/reliability/conflict/source_links）。
4. **信号计算**：重复/新颖/利用度/目标相关性/**可靠性**/**冲突度** 六信号量化 + MemoryScore。
5. **安全加固流程**：候选集→旧知识回放→增量训练→A/B 回归→旧性能≥阈值→批准固化（§4.1）。
6. **防灾难性遗忘**：碎片分区 + 回放巩固 + 弹性加固（§5.1），用持续学习基准回归验证。
7. 与 RTorch 自创仿脑模型（`RTORCH_MODEL.md`）的状态演化接口对齐。
