# Rust
target/
Cargo.lock.tmp

# Build/runtime artifacts regenerated by `cargo build --release` (build.rs)
*.exe
*.dll
*.pdb
*.d

# GPU kernels are compiled from examples/*.comp by build.rs -> target/release/kernels/
*.spv
*.spirv

# Data blobs (test inputs) — regenerate or keep tiny fixtures out of git
*.bin

# Compiler/bundled tools
tools/

# Editors / OS
.vscode/
.idea/
*.swp
Thumbs.db
.DS_Store
# MSVC / ASan artifacts
*.exp
*.ilk
*.lib
asanobj/
enginetest*
