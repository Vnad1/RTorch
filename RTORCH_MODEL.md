# RTorch 自创模型 v0 — 积分-放电"仿脑"状态模型

> 定位：RTorch 的**第一个自创模型**。走**仿脑（brain-inspired）**范式，但守住纪律——
> 向大脑的**功能原则**借鉴，不声称复刻特定解剖回路；全部用可计算、可验证的数学表达。
> 非 transformer，非现代深度学习栈。

## 理念（用户确立）

- **模型学的是状态，不是文本。** 文本/观测只是**填充在某个时间位置的占位物**，用于
  **驱动/扰动状态**，不是模型要记忆的东西。
- **参数 = 状态动力学的规则**，不是"词与词的统计"。
- 仿脑的关键点：记忆是**状态演化**（压缩进突触+当前活动），而非存储所有输入。

## 仿脑 = 借鉴大脑的功能原则，而非复刻生物结构

我们向大脑借鉴的是**组织原则**（可计算、可验证），不声称复刻真实神经元的解剖/生理：

| 借鉴的功能原则 | 模型里的实现 |
|---|---|
| 记忆是状态演化（非存储全部输入） | 状态 `v_t` 沿时间演化，只保留压缩状态 |
| 状态有界（不会无限膨胀） | 平方自限幅 `z - alpha·z²`（上界 = 1/(4·alpha)） |
| 衰减/遗忘 | 线性衰减项 `leak` |
| 跨通道耦合 | 状态转移矩阵 `A`（D×D） |
| 选择性/激活门槛 | 放电阈值 `theta`（过阈才触发） |

**边界**：不声称复刻特定神经回路、也不声称复现膜电位/离子通道的精确生物模型——
那是"神经仿真"，不是我们要的"仿脑计算模型"。

## 状态演化（离散时间欧拉积分一个漏电积分-放电神经元）

```
观测 u_t  ∈ ℝ^D    —— 文本/数据，填在 [B,T,D] 的 T 位置，注入状态
状态 v_t  ∈ ℝ^D    —— 膜电位（模型的记忆/活动）
演化 (v1: 含通道耦合 + 平方自限幅):
    z       = A·v[t-1] + drive·u[t]          (通道间耦合线性演化, A 是 D×D)
    v[t]    = z - leak·z - alpha·(z·z)        (线性衰减 + 平方自限幅)
    if v[t] > theta: fire → spike_count++, v[t] = v_reset   (过阈值放电+复位)
输出:
    final_state  : 每条序列末态（压缩记忆）
    spike_count  : 放电次数（活动量）
    peak         : 峰值电位（诊断）
```

### 平方自限幅的意义（本模型核心特性）

`z - alpha·z²` 是经典**单峰饱和**：求导极值在 `z = 1/(2·alpha)`，峰值上限 `= 1/(4·alpha)`。
即**状态存在一个可由 alpha 设定的自然上界**——膜电位不会无限增长（记忆不会爆炸），
像一个有"天花板"的神经元。**已验证**：alpha=0.02 → 上界 12.5，大驱动输入下 RTorch 峰值
被压在 ≤12.5 内，与参考逐值一致。这是平方函数在状态演化里的真实意义（自限幅/饱和），
而非摆设。

## 参数（= 状态动力学的可学习数字，v0 用固定常量）

| 参数 | 含义（仿脑对应） | v1 值 |
|---|---|---|
| `A` (D×D) | 通道间耦合（一个维度影响另一个） | 自0.9、跨0.05 |
| `alpha` | 平方自限幅强度（状态上界=1/(4·alpha)） | 0.02 |
| `leak` | 线性衰减/遗忘（膜电位回静息） | 0.05 |
| `drive` | 观测注入状态的增益 | 1.0 |
| `theta` | 放电阈值（激活门槛） | 1000.0（v0 几乎不放电） |
| `v_reset` | 放电后电位复位 | 0.0 |

### 已在本框架验证

`examples/spike_cell.cpp`（CPU 协议公式）：
- v1 前向：`[B=2,T=5,D=3]` 积分-放电 + 耦合 + 平方自限幅，输出与独立参考逐值一致。
- 平方饱和：`[B=2,T=50,D=3]` 大驱动（×5）输入，状态峰值被压在理论上限 12.5 内，
  与参考一致——平方项确实自限幅，状态不膨胀。

## 与"参数"叙事的衔接（回应"参数量干什么"）

- 本模型参数 = {leak, drive, theta, v_reset}（v0 标量），容量小但结构是"动力学"而非"文本统计"。
- 进阶后：状态内部可扩展为 A(D×D)、B(D×U)、C(O×D) 加可学习非线性——**参数规模由状态维度
  D 决定（主 A 的 D²），与序列长度 T 无关**（不随 T 爆炸，这是去掉文本全对全的根本收获）。

## 下一步（候选）

1. 把 `leak/drive/theta` 变成**可学习参数**（用 RTorch 公式内做梯度/或其更新），训一个小任务。
2. 状态演化升级为**连续时间微分方程**（`dv/dt`）+ 数值积分。
3. 多通道/多层状态（D 内部结构化），再沿 T 演化。
